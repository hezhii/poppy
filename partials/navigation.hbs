<ul class="c-navbar__list">
  {{#foreach navigation}}
    <li class="c-navbar__item {{#if current}}active{{/if}}">
      <a href="{{url absolute="true"}}" class="c-navbar__link">{{label}}</a>
    </li>
  {{/foreach}}
</ul>