{{!< default}}

{{#post}}
  <header class="site-header">
    {{> "site-nav"}}
    <div class="cover"
      {{#if feature_image}} style="background-image: url({{feature_image}}){{else}}no-cover{{/if}}"
    >
      <div class="container">
        <h1>{{title}}</h1>
      </div>
    </div>
    </div>
  </header>
{{/post}}